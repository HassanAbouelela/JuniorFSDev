---
apiVersion: v1
kind: ConfigMap
metadata:
    name: task-backend-config
    namespace: apps
data:
    APP_ENV: "production"
    DEPLOYMENT_PREFIX: "/api"
    LOG_LEVEL: "INFO"
    FORWARDED_ALLOW_IPS: "*"
    MOCK_AGENTS: "true"
    CORS_ORIGINS: |
        ["https://tasks.hassanamr.dev"]
